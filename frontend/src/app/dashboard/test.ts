export class Test {
    id:number
    start:string
    duration:number

    constructor(id:number, start:string, duration:number) {
        this.id = id;
        this.duration = duration;
        this.start = start;
    }
}